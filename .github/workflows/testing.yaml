name: CI-coverage
on: [push]
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 1
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v26
      - name: List all changed files
        run: |
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            echo "$file" >> .github/actions/api-test/diff_files
          done
          pwd
          cat .github/actions/api-test/diff_files
      - uses: php-actions/composer@v5
      - run: |
          php -v
          php -m.
          mkdir -p ./tests/Functional/extra
          php ./tests/Functional/console doctrine:database:create --env=test
          php ./tests/Functional/console d:s:u -f --env=test
      - name: PHPUnit Tests
        uses: php-actions/phpunit@v2
        with:
          php_extensions: xdebug
          bootstrap: ./tests/bootstrap.php
          args: --coverage-text --filter \$(cat .github/actions/api-test/diff_files | grep 'src/' | sed 's/.*\///' | sed 's/\.php//' | paste -sd '|' - )
        env:
          XDEBUG_MODE: coverage
          TEST_NAME: integration_test